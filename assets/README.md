# Assets

Versioned, redistributable project assets belong here. Generated audio and model weights do not.

The future `ambience/` library must include per-file provenance and license metadata. No asset may be published to Commons merely because it works locally.
